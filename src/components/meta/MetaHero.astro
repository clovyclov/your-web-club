---
---

<section class="meta-hero">
  <div class="container hero-container">
    <!-- Top Announcement Bar -->
    <div class="meta-announcement">
      <span class="announcement-text">Exclusively for Contractors, Landscapers, and Remodelers</span>
    </div>

    <!-- Main Headline with Clean Hierarchy -->
    <div class="hero-headline-wrapper">
      <h1 class="hero-title">
        Get An Automated High-Ticket Homeowner System That Guarantees <span class="highlight">10 Qualified Estimates</span> In 90 Days.
      </h1>
    </div>

    <!-- Video Embed Directly Under Headline -->
    <div class="hero-video-wrapper">
      <div class="video-container">
        <video 
          controls 
          playsinline 
          preload="metadata" 
          class="hero-video"
        >
          <source src="https://assets.cdn.filesafe.space/TSqcO2Er7wAliwNMEQpv/media/69f64ef06b07ab33031b7de7.mp4" type="video/mp4" />
          Your browser does not support the video tag.
        </video>
      </div>
    </div>

    <!-- Subheadline -->
    <p class="hero-subtitle">
      For just <strong>$297/mo</strong>, get an automated high-converting trade website, instant 5-minute SMS lead dispatch, and automatic Google review engine. 
      If we don't deliver 10 qualified homeowner estimate opportunities in your first 90 days, <strong>we work 100% free until we do.</strong>
    </p>

    <!-- CTAs & Guarantee Callout -->
    <div class="cta-wrapper">
      <a href="#claim-offer" class="btn btn-neon btn-lg">CLAIM YOUR 90-DAY GUARANTEE NOW</a>
      <p class="cta-subtext">No 12-month lock-in contract • Setup in as little as 7 days</p>
    </div>

    <!-- Key Trust Badges -->
    <div class="trust-badges">
      <div class="badge-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"></polyline></svg>
        <span>Ready in 7 days or less</span>
      </div>
      <div class="badge-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"></polyline></svg>
        <span>100% Exclusive Homeowner Leads</span>
      </div>
      <div class="badge-item">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"></polyline></svg>
        <span>Work-For-Free Guarantee</span>
      </div>
    </div>
  </div>
</section>

<style>
  .meta-hero {
    padding: 4.5rem 0 4rem 0;
    text-align: center;
  }

  .hero-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2rem;
    max-width: 1020px;
  }

  /* Announcement Bar */
  .meta-announcement {
    display: inline-flex;
    align-items: center;
    background: #262626;
    border: 2.5px solid #000000;
    box-shadow: 4px 4px 0px #000000;
    padding: 0.75rem 1.85rem;
    border-radius: 0;
    margin-bottom: -0.75rem;
  }

  .announcement-text {
    font-family: var(--font-body);
    font-size: 1.05rem;
    font-weight: 700;
    color: #ffffff;
    letter-spacing: 0.02em;
  }

  /* Headline Hierarchy Improvements */
  .hero-headline-wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.85rem;
    max-width: 940px;
  }

  .hero-kicker {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    font-weight: 800;
    text-transform: uppercase;
    color: var(--btn-orange);
    letter-spacing: 0.02em;
    background: rgba(249, 108, 44, 0.12);
    border: 1px solid rgba(249, 108, 44, 0.3);
    padding: 0.35rem 1.1rem;
    display: inline-block;
  }

  .hero-title {
    font-family: var(--font-heading);
    font-size: 2.35rem;
    font-weight: 800;
    line-height: 1.35;
    color: var(--text-white);
    letter-spacing: -0.01em;
    text-transform: none; /* Title Case for vastly superior readability */
  }

  .hero-title .highlight {
    color: var(--neon-green);
    font-weight: 900;
    text-decoration: underline;
    text-decoration-color: var(--neon-green);
    text-underline-offset: 6px;
    text-decoration-thickness: 3px;
  }

  /* Video Embed Styling */
  .hero-video-wrapper {
    width: 100%;
    max-width: 820px;
    margin: 0.5rem 0 0.5rem 0;
  }

  .video-container {
    position: relative;
    width: 100%;
    background: #000000;
    border: 2.5px solid #000000;
    box-shadow: 6px 6px 0px #000000;
    border-radius: 0;
    overflow: hidden;
    aspect-ratio: 16 / 9;
  }

  .hero-video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .hero-subtitle {
    font-size: 1.15rem;
    color: #cbd5e1;
    max-width: 820px;
    line-height: 1.65;
  }

  .hero-subtitle strong {
    color: var(--text-white);
  }

  .cta-wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    margin-top: 0.5rem;
  }

  .btn-lg {
    padding: 1.2rem 3rem;
    font-size: 1.05rem;
    letter-spacing: 0.03em;
  }

  .cta-subtext {
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  .trust-badges {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 2rem;
    margin-top: 1rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    width: 100%;
  }

  .badge-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.9rem;
    font-weight: 700;
    color: #e2e8f0;
  }

  .badge-item svg {
    width: 18px;
    height: 18px;
    stroke: var(--btn-orange);
  }

  @media (max-width: 768px) {
    .hero-kicker {
      font-size: 1rem;
    }
    .hero-title {
      font-size: 1.65rem;
      line-height: 1.3;
    }
    .hero-subtitle {
      font-size: 1rem;
    }
    .trust-badges {
      gap: 1rem;
      flex-direction: column;
      align-items: center;
    }
  }
</style>
